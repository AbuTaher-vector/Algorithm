#include<bits/stdc++.h>
using namespace std;
#define nn int
#define dd double
#define ll long long int
#define ss string
#define nl '\n'
int main()
{

    /* GCD means greatest common Divisor and LCM means Least Common Multiple.
    hint 1 : a*b = LCM(a,b)*GCD(a,b)
    hint 2 : LCM(a,b) = a*b/GCD(a,b)
    */
    nn a,b;
    cin>>a>>b;
    cout<<"GCD of "<<a<<" and "<<b<<" is "<<__gcd(a,b)<<nl; 
    cout<<"LCM of "<<a<<" and "<<b<<" is "<<(a*b)/__gcd(a,b)<<nl; 

}
