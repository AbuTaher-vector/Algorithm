#include<bits/stdc++.h>
using namespace std;
#define nn int
#define dd double
#define ll long long int
#define ss string
#define nl '\n'
int main()
{
     nn v=5;
    nn arr[v+1][v+1];
    for(nn i=0;i<v;i++) 
       {
          for(nn j=0;j<v;j++) arr[i][j]=0;
              
       }
    arr[0][1]=1;
     arr[0][4]=1;
      arr[1][2]=1;
       arr[1][3]=1;
        arr[1][4]=1;
         arr[2][3]=1;
          arr[3][4]=1;
          cout<<"   ";
          //this representation calls adjacency matrix
      for(nn i=0;i<v;i++) cout<<i<<" ";
          cout<<nl<<nl;
       for(nn i=0;i<v;i++) 
       {
          cout<<i<<"  ";
          for(nn j=0;j<v;j++) cout<<arr[i][j]<<" ";
               cout<<nl;
       }
          cout<<nl;
     

}
